# This is a basic workflow to help you get started with Actions

name: Deployment GitHub Pages

on:
  push:
    branches: [ master ]
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Install Dependencies
        run: npm install

      - name: Build
        run: |
          git config --global user.email "57884979+s1mpleTEK@users.noreply.github.com"
          git config --global user.name "s1mpleTEK"
          npm run deploy
        
      
